/**
 * Generated by "@vuepress/plugin-blog"
 */
export default {
  spell: function (page, id, pid) {
                return page.pid === pid && page.id === id;
            },
  monster: function (page, id, pid) {
                return page.pid === pid && page.id === id;
            },
  magicitem: function (page, id, pid) {
                return page.pid === pid && page.id === id;
            },
}